[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pip install -e . && uvicorn energy_analytics.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
